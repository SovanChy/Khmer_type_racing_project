# npm install runs postinstall scripts with full user permissions; this is
# the cheapest supply-chain control available (F-04). If a package genuinely
# needs its install script, re-enable it per-package and record the reason.
ignore-scripts=true
