// Phase 1 is the cluster engine only — this is a placeholder so `npm run build`
// has an entry point. The typing UI arrives in Phase 3.
export function App() {
  return (
    <main className="grid min-h-dvh place-items-center bg-neutral-950 text-neutral-400">
      <p className="text-sm">Phase 1: Khmer cluster engine. No UI yet.</p>
    </main>
  );
}
